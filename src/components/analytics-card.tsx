export default function AnalyticCard({
	title,
	value,
	variant,
	increateValue,
}: {
	title: string;
	value: number;
	variant: "up" | "down";
	increaseValue: number;
}) {
	return <div>Analytics Card</div>;
}
